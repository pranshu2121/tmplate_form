import { CustomFieldDirective } from './custom-field.directive';

describe('CustomFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFieldDirective();
    expect(directive).toBeTruthy();
  });
});
